# ── PocketOption ──────────────────────────────────────────────────────────────
pocketoptionapi>=1.0.0          # Conector WebSocket PocketOption (SSID auth)
websocket-client>=1.6.0         # Dependência do pocketoptionapi

# ── UI e Dashboard ────────────────────────────────────────────────────────────
streamlit>=1.35.0               # Painel de controle (app_streamlit.py)

# ── Banco de dados ────────────────────────────────────────────────────────────
sqlalchemy>=2.0.0               # ORM PostgreSQL
psycopg2-binary>=2.9.0          # Driver PostgreSQL
python-dotenv>=1.0.0            # Carregamento do .env

# ── Data e Analysis ───────────────────────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0

# ── AI / Otimização ───────────────────────────────────────────────────────────
anthropic>=0.40.0               # Level 4D — Claude API meta-review
optuna>=3.5.0                   # Level 4B — Bayesian hyperparameter search

# ── Deep Learning (Strategy 3 LSTM — opcional) ────────────────────────────────
# torch>=2.0.0                  # Descomentar se usar Strategy 3 LSTM

# ── Referência Alpaca (estratégias herdadas — opcional) ───────────────────────
# alpaca-py>=0.20.0             # Descomentar se usar Strategy 2/3 em Alpaca
